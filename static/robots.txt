User-agent: *
Allow: /

# Legacy Squarespace query URLs (?source=, ?tag=, ?category=, ?author=) are
# deliberately not disallowed: they serve pages whose canonical drops the
# query string, and Google can only merge them into the canonical URL if it
# is allowed to crawl them. Blocking would keep them indexed as bare URLs.

Sitemap: https://collado.io/sitemap-0.xml
